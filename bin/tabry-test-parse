#!/usr/bin/env ruby
require_relative '../lib/tabry/runner'

raise 'Usage: tabry-test-parse config_name arg1 [arg2]...' unless ARGV.length >= 2

config_name, *rest = ARGV
puts Tabry::Runner.new(config_name: config_name).parse(args)
puts state
